<div class="announcement{% if section.settings.show_prelaunch_announcement %} announcement--prelaunch{% endif %}{% if section.settings.announcement_closable %} announcement--closeable announcement--closed{% endif %}">
  {%- if section.settings.show_prelaunch_announcement -%}
    <div class="announcement__prelaunch-content">
      <span class="announcement__text announcement__text--open" data-text="{{ section.settings.announcement_text | strip_html | handle }}">
        {{ section.settings.announcement_text | strip_html }}
      </span>

      {%- if section.settings.announcement_link != blank and section.settings.prelaunch_button_label != blank -%}
        <a href="{{ section.settings.announcement_link }}" class="announcement__prelaunch-button">
          {{ section.settings.prelaunch_button_label | escape }}
        </a>
      {%- endif -%}
    </div>
  {%- else -%}
    {%- if section.settings.announcement_link != blank -%}
      <a href="{{ section.settings.announcement_link }}" class="announcement__link">
    {%- endif -%}

      <span class="announcement__text{% unless section.settings.announcement_closable %} announcement__text--open{% endunless %}" data-text="{{ section.settings.announcement_text | strip_html | handle }}">
        {{ section.settings.announcement_text | strip_html }}
      </span>

    {%- if section.settings.announcement_link != blank -%}
      </a>
    {%- endif -%}
  {%- endif -%}

  {%- if section.settings.announcement_closable -%}
    <button type="button" class="text-link announcement__close">
      <svg aria-hidden="true" focusable="false" role="presentation" class="icon icon-close" viewBox="0 0 64 64"><title>icon-X</title><path d="m19 17.61 27.12 27.13m0-27.12L19 44.74"/></svg>
      <span class="icon__fallback-text">{{ 'general.accessibility.close' | t | json }}</span>
    </button>
  {%- endif -%}
</div>

{% schema %}
  {
    "name": "t:sections.header.settings.header_announcement_bar",
    "settings": [
      {
        "type": "text",
        "id": "announcement_text",
        "label": "t:sections.header.settings.announcement_text.label",
        "default": "Free shipping and returns",
        "info": "t:sections.header.settings.announcement_text.info"
      },
      {
        "type": "url",
        "id": "announcement_link",
        "label": "t:sections.header.settings.announcement_link.label"
      },
      {
        "type": "checkbox",
        "id": "show_prelaunch_announcement",
        "label": "Show pre-launch announcement style",
        "default": false
      },
      {
        "type": "text",
        "id": "prelaunch_button_label",
        "label": "Pre-launch button label",
        "default": "Reserve Now"
      },
      {
        "type": "checkbox",
        "id": "announcement_closable",
        "label": "t:sections.header.settings.announcement_closable.label"
      }
    ]
  }
{% endschema %}
